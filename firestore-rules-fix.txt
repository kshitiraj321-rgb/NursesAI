rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Secure the user's entire document and all subcollections (like topicProgress, quizResults, mistakeBank)
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Add other top-level collections if any, for example:
    // match /publicData/{docId} {
    //   allow read: if true;
    // }
  }
}
